root = "."
tmp_dir = "tmp"

[build]
  cmd = "CGO_ENABLED=1 go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  full_bin = "./tmp/api"
  include_ext = ["go", "sql"]
  exclude_dir = ["tmp", "data", "testdata", "vendor"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
